{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "emitDeclarationOnly": true,
    // 只输出声明文件(ts 产物)
    "declaration": true,
    // 自动生成声明文件
    "declarationDir": "dist"
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.js",
    "src/**/*.d.ts"
  ]
}